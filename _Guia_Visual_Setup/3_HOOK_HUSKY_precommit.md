# ¿PARA QUÉ ES ESTO?
Es para que Graphify actualice su mapa de conocimiento cada vez que guardas código (commit).

**DÓNDE VA:** Guárdalo en `.husky/pre-commit`
*(Recuerda haber ejecutado `bun add -D husky` y `bunx husky init` antes)*

---

```bash
npx graphify .
git add graph.json
```
